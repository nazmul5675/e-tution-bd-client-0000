import React from 'react';

const ProfileSettings = () => {
    return (
        <div>
            Profile SettingsProfile SettingsProfile Settings
        </div>
    );
};

export default ProfileSettings;